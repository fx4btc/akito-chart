import { Attribute, Property } from "./element";
import { Button } from "./button";
export interface Switch extends Instance, Button {
}
export interface Class {
    new (option?: Option): Switch;
    prototype: Instance;
}
export interface Instance {
    isOn(): boolean;
    switchOn(): this;
    switchOff(): this;
    toggleSwitch(): this;
}
export interface Option {
    /** id attribute. */
    id?: string;
    /** class attribute. */
    className?: string;
    /** attribute/value pairs properties. */
    attribute?: Attribute;
    /** CSS style properties (uses flagrate.Element.setStyle). */
    style?: Property;
    /** default is false. */
    isFocused?: boolean;
    /** default is false. */
    isDisabled?: boolean;
    /** default is false. */
    isOn?: boolean;
}
export declare const Switch: Class;
export declare function createSwitch(option?: Option): Switch;
